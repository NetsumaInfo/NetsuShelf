# Chrome Web Store Submission Pack

Prepared for NetsuShelf on March 11, 2026.

This file gives copy-ready text for the Chrome Web Store Developer Dashboard.

## Store Listing

### Language

- Use `English (en)` for the first submission

### Title

- `NetsuShelf`

### Summary from package

- Comes from `plugin/manifest.json`
- New package summary:
  `Convert supported web novels and story pages into EPUB files for offline reading.`

### Description

Use this text in the `Description` field:

```text
NetsuShelf turns supported web novels and story pages into clean EPUB files for offline reading.

Open a supported story page, let NetsuShelf analyze the title, author, cover, and chapter list, then export the chapters you want as an EPUB. You can choose chapter ranges, fine-tune metadata, select or replace the cover image, and keep optional library entries locally in the extension for later updates.

NetsuShelf is built for reader-controlled exports. Processing happens in the browser and the extension only contacts the sites needed to fetch the pages, images, and optional metadata that you explicitly request.
```

### Category

- `Productivity`

### Official URL

- Select `None` unless you later publish on a domain you own

### Promotional video

- Leave empty

## Images

Chrome's current image requirements are documented here:

- https://developer.chrome.com/docs/webstore/images/

Use these files:

- Store icon: `doc/store/chrome-icon-128.png`

Optional images for a first submission:

- Small promo image: leave empty unless you want richer merchandising
- Marquee promo image: leave empty unless you want a featured store layout

### Screenshots

At least one screenshot is mandatory. Use a real capture of the current NetsuShelf UI, not the old `WebToEpub` screenshot from the repo.

Recommended screenshots to upload:

1. Main analysis screen on a supported story page
2. Chapter selection range screen with the numeric `No.` inputs visible
3. Optional library or advanced metadata screen

Current Chrome sizes accepted by the store for screenshots:

- `1280x800`
- `640x400`

## Privacy Practices

Chrome's privacy guidance:

- https://developer.chrome.com/docs/webstore/cws-dashboard-privacy/
- https://developer.chrome.com/docs/webstore/program-policies/privacy

### Single purpose description

```text
Convert the story page selected by the user into an EPUB file for offline reading, including chapter selection, metadata cleanup, cover handling, and optional local library storage.
```

### Permission justifications

Use these justifications in the Privacy practices tab:

- `cookies`
  `Used to fetch chapters or images from the site selected by the user when that site requires browser cookies for access.`

- `downloads`
  `Used to save the EPUB file generated by the extension to the user's device.`

- `webRequest`
  `Used for request compatibility on supported sites and to support request handling needed by the user-selected source page.`

- `declarativeNetRequest`
  `Used to adjust request behavior needed to load chapters or images from supported sites selected by the user.`

- `scripting`
  `Used to inject the content script into the current tab so the extension can analyze the page the user chose.`

- `storage`
  `Used to save local preferences and optional local library data inside the browser.`

- `unlimitedStorage`
  `Used to store optional local library entries and EPUB data without running into the default extension storage limit.`

- `<all_urls>`
  `Required because NetsuShelf works across many supported story sites and must analyze and fetch content from the site currently chosen by the user.`

### Remote code

- Select `No, I am not using remote code`

### Data usage

Recommended disclosure:

- Mark `Website content`
- Mark `Web browsing activity`
- If the dashboard separately lists `Authentication information`, mark it because the extension can access site cookies transiently to fetch user-requested content from supported sites

Recommended certification answers:

- No sale of user data
- No use for unrelated advertising
- No use for creditworthiness or lending decisions

### Privacy policy URL

Use this URL after the branch is merged or made permanent:

- `https://github.com/NetsumaInfo/NetsuShelf/blob/codex/Prototype/PRIVACY.md`

If you later merge to the default branch, replace the branch name in the URL.

## Distribution

- Visibility: `Public`
- Regions: `All regions` unless you want a staged launch
- Pricing: `Free`

## Test Instructions

Use this in the `Test instructions` field:

```text
No account is required.

Primary test page:
https://www.royalroad.com/fiction/21220/mother-of-learning

How to test:
1. Open the page above.
2. Click the NetsuShelf extension icon.
3. Wait for the extension to analyze the page and populate the title, author, cover, and chapter list.
4. In the chapter selection section, type a chapter number into the `No.` field for First Chapter or Last Chapter and verify that the matching chapter is selected.
5. Click `Pack EPUB` and confirm that an EPUB download is created.

What to expect:
- The extension reads the selected story page, builds a chapter list, and exports the selected range as an EPUB.
- Preferences and optional library entries are stored locally in the browser.
```

## Review Notes

- The package should be rebuilt and re-uploaded after the manifest description change if you want the package summary to appear in the dashboard.
- Do not reuse the old repo screenshot that still shows `WebToEpub`.
- For the first pass, you can submit without the optional promo and marquee images.
